# Copy this to .env and fill values. Do NOT commit secrets.
FLASK_APP=application
FLASK_DEBUG=1
# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=book_review
DB_HOST=localhost
DB_PORT=5432
# Full URL (optional, overrides DB_* vars)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/book_review
# App
SECRET_KEY=change-me-in-production
# pgAdmin
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
FLASK_APP=application.py
FLASK_DEBUG=1

# --- Database Credentials ---
DB_USER=postgres
DB_PASSWORD=your_strong_password_here
DB_NAME=book_review
DB_HOST=db
DB_PORT=5432

DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Goodreads API (nếu còn dùng)
# GOODREADS_KEY=your_key_here

# pgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=your_pgadmin_password_here
